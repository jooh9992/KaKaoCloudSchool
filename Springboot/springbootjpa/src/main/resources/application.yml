server:
    port: 80

# ?????? ?? ??
spring:
    datasource:
        driver-class-name: oracle.jdbc.driver.OracleDriver
        url: jdbc:oracle:thin:@localhost:1521:xe
        username: system
        password: oracle
    #Entity? ??? ???? ????? ??? ??: create(??? ? ??? ?? ??), create-drop(??? ? ???? ??? ? ??),
        ##update(???? ??), validate(??? ??), none(???? ??)
    jpa:
        hibernate:
            ddl-auto: update
        #show_sql? sql ??? ???? ??? format_sql? ??? ???? ??
        properties:
          hibernate:
              format_sql: true
              show_sql: true
#? ? ?? ???? ? ? ?? ??
logging:
    level:
      org.hibernate.type.descriptor.sql: trace
